export enum ControlType {
    STRING = 'string',
    NUMBER = 'number',
    VECTOR2 = 'vector2',
    BOOLEAN = 'boolean',
    COLOR = 'color',
    TEXTURE = 'texture',
    SIDES = 'sides',
    SEPARATOR = 'separator',
    TEXTAREA = 'textarea',
    RADIOBUTTONS = 'radiobuttons',
    SELECT = 'select',
}
